const app = require('./')

describe('import', () => {
  it('should find one', () => {
    const input = 'z'
    const result = app(input)
    expect(result).toBe('x')
  })

  it('should find many')
})

describe('require', () => {
  it('should find one')

  it('should find many')
})
